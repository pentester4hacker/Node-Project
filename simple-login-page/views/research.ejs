<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Research Choices</title>
    <!-- Include necessary meta tags, stylesheets, and other dependencies -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        
        body {
            background-color: #000;
            background-image: url(images/dashboard1.gif);
            animation: backgroundAnimation 20s infinite alternate;
            color: #fff; 
            position: relative;
        }

        @keyframes backgroundAnimation {
            0% { background-position: 0% 50%; }
            100% { background-position: 100% 50%; }
        }
        .subject-box {
            padding: 20px;
            border-radius: 10px;
            margin: 10px;
            transition: transform 0.3s ease;
            cursor: pointer;
            background-color: rgba(255, 218, 185, 0.5); 
            color: #000; 
            width: 300px; 
            height: 150px; 
            text-align: center;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
        }

        .subject-box:hover {
            transform: scale(1.05);
            background-color: #9c5d42; 
        }

        .container {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1 style="color:#9c5d42;">Research Choices</h1>  
        <div class="subjects" style="display: flex; flex-wrap: wrap; justify-content: center;"> 
            <% researchChoices.forEach(choice => { %>
                <div class="subject-box">
                    <h3><%= choice.research_subject %></h3>
                </div>
            <% }) %>
        </div>
    </div>
</body>
</html>
